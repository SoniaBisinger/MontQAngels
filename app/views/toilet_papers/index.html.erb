
<%= render "navbar_wagon" %>

hello world !
<br/>
<%= link_to "Return", root_path, class: "btn btn-flat" %>
<br/>
<div class="bar_recherche">
  <%= simple_form_for :search, url: root_path, method: :get do |f| %>
    <div class="search-form-control form-group">
      <input class="form-control string required" type="text" name="search[query]" id="search_query" />
      <button name="button" type="submit" class="btn btn-flat">
        <i class="fas fa-search"></i> Search
      </button>
    </div>
  <% end %>
</div>

<%#MANQUE LA BARRE DES FILTRES %>

<div>
  /////////////////////
  <%= ToiletPaper.count %>
  <% @toilet_papers.each do |paper| %>
    <%= paper.title %>
  <% end %>
  ///////////////////

  <% @toilet_papers.each do |paper| %>
    <%= paper.title %>
    <%= link_to toilet_paper_path(toilet_paper), class:"text-decoration-none" do %>
        <div class="card-category" data-aos="zoom-in" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path('list_background.jpg') %>)">
        <%= list.name %>
      </div>
    <% end %>
  <% end %>
</div>
<br/>
<br/>
<%= render "footer" %>
